# Infrastructure Environment Configuration
# Copy this file to .env.db and customize the values

# PostgreSQL Database
POSTGRES_DB=laberis
POSTGRES_USER=laberis_user
POSTGRES_PASSWORD=change-this-secure-database-password

# MinIO Object Storage
MINIO_ROOT_USER=laberis_minio
MINIO_ROOT_PASSWORD=change-this-secure-minio-password

# IMPORTANT: MinIO credentials must match MinIO__AccessKey/MinIO__SecretKey in .env.prod